# Сервис управления рассылками

Этот проект представляет собой веб-приложение на Django для управления рассылками сообщений клиентам.

---

## Текущая функциональность

### 1. Управление клиентами
- Добавление, просмотр, редактирование и удаление клиентов.
- Модель `Client` с полями: `email`, `full_name`, `comment`.

### 2. Управление сообщениями
- Добавление, просмотр, редактирование и удаление сообщений.
- Модель `Message` с полями: `subject`, `body`.

### 3. Управление рассылками
- Добавление, просмотр, редактирование и удаление рассылок.
- Модель `Mailing` с полями: `start_time`, `end_time`, `status`, `message`, `clients`.
- Статусы рассылки: `Создана`, `Запущена`, `Завершена`.

### 4. Отправка сообщений по требованию
- Возможность отправки рассылки вручную через интерфейс пользователя (кнопка "Отправить").
- Логика сохранения попыток отправки в модели `MailingAttempt`.

### 5. Попытки рассылок
- Модель `MailingAttempt` с полями: `attempt_time`, `status`, `server_response`, `mailing`.

### 6. Главная страница
- Отображение количества всех рассылок, активных рассылок и уникальных получателей.

---

## Установка и запуск

### Требования
- Python 3.11+
- Django 5.2+

### Установка
1. Клонируй репозиторий:
   ```bash
   git clone <ссылка_на_твой_репозиторий>
   cd mailing_service

2. Создай и активируй виртуальное окружение:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Для Windows: venv\Scripts\activate

3. Установи зависимости:
   ```bash
   pip install -r requirements.txt

4. Выполни миграции:
   ```bash
   python manage.py migrate

5. Запусти сервер:
   ```bash
   python manage.py runserver

6. Перейди по адресу http://127.0.0.1:8000/ в браузере.

---

## Структура проекта

   ```
mailing_service/
├── mailing/                  # Основное приложение
│   ├── migrations/           # Миграции базы данных
│   ├── templates/            # Шаблоны
│   │   └── mailing/          # Шаблоны приложения
│   ├── admin.py              # Настройки админ-панели
│   ├── apps.py               # Конфигурация приложения
│   ├── forms.py              # Формы
│   ├── models.py             # Модели
│   ├── services.py           # Логика отправки сообщений
│   ├── urls.py               # Маршруты приложения
│   └── views.py               # Представления
├── mailing_service/          # Конфигурация проекта
│   ├── settings.py           # Настройки проекта
│   ├── urls.py               # Главные маршруты
│   └── wsgi.py
├── manage.py                 # Управление проектом
└── README.md                 # Этот файл
   ```

